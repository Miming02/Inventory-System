PORT=8787

# PostgreSQL (external DB managed via pgAdmin)
PGHOST=localhost
PGPORT=5432
PGDATABASE=Inventory_system
PGUSER=inventory_api
PGPASSWORD=Miming0214

# Prisma (preferred)
DATABASE_URL=postgresql://inventory_api:Miming0214@localhost:5432/Inventory_system?schema=public

